const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.site-nav');if(toggle&&nav)toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});

const filterButtons=[...document.querySelectorAll('.filter-button')],posts=[...document.querySelectorAll('.blog-row')],search=document.querySelector('#article-search'),empty=document.querySelector('.empty-state'),count=document.querySelector('#article-count');function filterArticles(){if(!posts.length)return;const value=g=>document.querySelector(`[data-group="${g}"].active`)?.dataset.value||'all',tag=value('tag'),month=value('month'),category=value('category'),query=(search?.value||'').trim().toLowerCase();let visible=0;posts.forEach(post=>{const show=(tag==='all'||post.dataset.tags.split(' ').includes(tag))&&(month==='all'||post.dataset.month===month)&&(category==='all'||post.dataset.category===category)&&(!query||post.textContent.toLowerCase().includes(query));post.hidden=!show;if(show)visible++});if(empty)empty.style.display=visible?'none':'block';if(count)count.textContent=visible}filterButtons.forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll(`[data-group="${button.dataset.group}"]`).forEach(b=>b.classList.remove('active'));button.classList.add('active');filterArticles()}));search?.addEventListener('input',filterArticles);

const galleryFilters=[...document.querySelectorAll('[data-gallery-filter]')],galleryItems=[...document.querySelectorAll('[data-gallery-category]')];galleryFilters.forEach(button=>button.addEventListener('click',()=>{const selected=button.dataset.galleryFilter;galleryFilters.forEach(b=>b.classList.toggle('active',b===button));galleryItems.forEach(item=>item.hidden=selected!=='all'&&!item.dataset.galleryCategory.split(' ').includes(selected))}));if(location.hash&&galleryFilters.length){document.querySelector(`[data-gallery-filter="${location.hash.slice(1)}"]`)?.click()}
